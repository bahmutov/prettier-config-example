// typical Mocha test suite
/* global expect */
describe('Mocha tests', () => {
  it('has a test', () => {
    expect(1).to.equal(1)
  })

  it('has another test', () => {
    expect(2).to.equal(2)
  })
})
